# Pinned for reproducible builds. Update with: pip install -r requirements.txt --upgrade && pip freeze.
Django>=4.2,<5
python-dotenv>=1.0
pandas>=2.0
openpyxl>=3.1
matplotlib>=3.7
Pillow>=10.0
openai>=1.0
reportlab>=4.0
svglib>=1.5
python-docx>=1.0
gunicorn>=21.0
